
화면의 사용자에게 보이지는 않지만, 서버측에는 전달해야하는 값들을 다룰 때 hidden 사용 


css

선택자 라는 개념이 가장 중요하다. 


자손 선택자 : 바로 다음 레벨만 선택 
후손 선택자 : 다음 레벨 밑에 다음 레벨이 더 있으면 전체를 선택

float 을 사용하면 사용한 이후에는 전부 float이 적용된다. 
->  
<br clear="both"> 하고 clear해줘야 한다. 